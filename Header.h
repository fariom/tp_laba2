#pragma once
#include <iostream>
#include <fstream>
#include <locale.h>
#include <Windows.h>

using namespace std;

char* copyWord(char* orig);
int getLength(char* line);
void pressEnter();
void input(int* num);
